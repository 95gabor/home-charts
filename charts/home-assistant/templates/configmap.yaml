apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "home-assistant.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
data:
  configuration.yaml: |
    {{- .Values.homeAssistant.configuration | nindent 4 }}
