apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "samba.fullname" . }}
  labels:
    {{- include "samba.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace | quote }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "samba.selectorLabels" . | nindent 6 }}
  serviceName: {{ include "samba.fullname" . }}
  template:
    metadata:
      annotations:
        {{- with .Values.podAnnotations }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "samba.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.terminationGracePeriodSeconds }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      {{- end }}
      {{- if .Values.hostNetwork }}
      hostNetwork: {{ .Values.hostNetwork }}
      {{- end }}
      {{- with .Values.dnsConfig }}
      dnsConfig:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      dnsPolicy: {{ .Values.dnsPolicy | default "ClusterFirst" }}
      initContainers:
        - name: init-directories
          image: busybox:1.36
          securityContext:
            runAsUser: 0
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
              add:
                - CHOWN
                - FOWNER
                - MKNOD
          command:
            - sh
            - -c
            - |
              # Create Samba state directories with proper permissions
              mkdir -p /var/lib/samba/private
              mkdir -p /var/cache/samba
              mkdir -p /run/samba
              chmod -R 0700 /var/lib/samba
              chmod -R 0700 /var/cache/samba
              chmod -R 0700 /run/samba
              # Create share directories (extract from SAMBA_VOLUME_CONFIG env vars)
              # This creates common Time Machine share directory
              mkdir -p /shares/timemachine
              chmod 0755 /shares/timemachine
          volumeMounts:
            - name: shares
              mountPath: /shares
            - name: var-lib-samba
              mountPath: /var/lib/samba
            - name: var-cache-samba
              mountPath: /var/cache/samba
            - name: run-samba
              mountPath: /run/samba
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            {{- $envExtraNames := dict }}
            {{- if .Values.envExtra }}
            {{- range .Values.envExtra }}
            {{- $_ := set $envExtraNames .name true }}
            {{- end }}
            {{- end }}
            {{- range $key, $value := .Values.env }}
            {{- if not (hasKey $envExtraNames $key) }}
            - name: {{ $key | quote }}
              value: {{ $value | quote }}
            {{- end }}
            {{- end }}
            {{- if .Values.envExtra }}
            {{- range .Values.envExtra }}
            - name: {{ .name | quote }}
              {{- if .value }}
              value: {{ .value | quote }}
              {{- end }}
              {{- if .valueFrom }}
              valueFrom:
                {{- toYaml .valueFrom | nindent 16 }}
              {{- end }}
            {{- end }}
            {{- end }}
            {{- with .Values.envFrom }}
            envFrom:
                {{- toYaml . | nindent 10 }}
            {{- end }}
          volumeMounts:
            - name: shares
              mountPath: /shares
            - name: var-lib-samba
              mountPath: /var/lib/samba
            - name: var-cache-samba
              mountPath: /var/cache/samba
            - name: run-samba
              mountPath: /run/samba
          ports:
            - name: udp137
              containerPort: 137
              protocol: UDP
            - name: udp138
              containerPort: 138
              protocol: UDP
            - name: tcp139
              containerPort: 139
              protocol: TCP
            - name: tcp445
              containerPort: 445
              protocol: TCP
          livenessProbe:
            tcpSocket:
              port: tcp445
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: tcp445
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: shares
          persistentVolumeClaim:
            claimName: {{ include "samba.fullname" . }}
        - name: var-lib-samba
          emptyDir: {}
        - name: var-cache-samba
          emptyDir: {}
        - name: run-samba
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
